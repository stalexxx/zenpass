{
  "name": "crypto-wasm",
  "private": true,
  "type": "module",
  "exports": { ".": "./pkg/crypto_wasm.js" },
  "files": ["pkg/"],
  "scripts": {
    "build:wasm": "cargo build --manifest-path Cargo.toml --target wasm32-unknown-unknown --release && /Users/admin/.cargo/bin/wasm-bindgen target/wasm32-unknown-unknown/release/crypto_wasm.wasm --target web --out-dir pkg --typescript",
    "test": "bun run build:wasm && bun test"
  }
}
