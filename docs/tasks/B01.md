# B01: Rust crypto-core
Goal: Implement only approved `crypto-envelope/v1` and OPAQUE helper API in Rust.
Dependencies: H01.
Allowed paths: `crates/crypto-core/`, `crates/crypto-core-tests/`.
Forbidden paths: TypeScript clients, backend, contracts.
Inputs and approved contracts: `docs/contracts/crypto-envelope-v1.md`, `fixtures/crypto/`.
Required implementation: Key hierarchy, XChaCha20-Poly1305, Argon2id, zeroization, typed errors, property tests, fuzz targets, and no secret-bearing debug output.
Acceptance criteria: All vectors pass and malformed input fails safely.
Required tests: Unit, property, fuzz smoke, vectors, memory/zeroization review.
Commands to verify: `cargo test --workspace`; `cargo fmt --check`; `cargo clippy -- -D warnings`.
Deliverables: Rust crate, API docs, test report.
Risks and escalation conditions: Stop on vector ambiguity or library security concern.
Completion report format: Standard completion report.

