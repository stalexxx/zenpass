# Live status

This file is maintained by the integration agent.

| ID | Status | Owner | Dependencies | Last result |
|---|---|---|---|---|
| A00 | MERGED | foundation agent | — | Scaffold builds; boundary check and Rust tests pass |
| A01 | MERGED | security agent | A00 | 20 threats mapped to mitigations and verification |
| A02 | MERGED | contracts agent | A00 | OpenAPI, schemas, fixtures, and sync state machine added |
| A03 | MERGED | coordinator | A00 | UX flows and accessibility states documented |
| A04 | MERGED | integration agent | A01,A02 | **Integrator correction, 2026-09-05:** this row was stuck on a stale "ready for H01" note after H01 already ran and closed it — A04's own deliverable ("frozen crypto contract pending H01") was met when the human reviewer approved ADR-0003 with findings on 2026-09-03. Rework scope (G-01, G-02, G-04, G-09, G-11) is complete (dependency-free `packages/crypto-fixtures/` Bun harness; recovery placeholder resized to 48 B). The remaining gaps (G-03, G-05, G-06, G-07, G-10) were not left as open A04 work — ADR-0003 formally disposed them as findings F-2/F-3, "accepted only to start B01/B04 implementation," and reassigned to B01 (canonical-CBOR enforcement, adapter-backed vector verification, calibration recording — merged) and H02 (full transitive dependency/audit-applicability closure — still open). No further A04-scoped action remains; G-06/F-3's audit-applicability gap is H02's, not A04's. |
| H01 | MERGED | integration agent | A04 | `stalexxx` approved with findings on 2026-09-03; ADR-0003 freezes v1 and defers F-2/F-3 to B01/A04/H02 |
| B01 | MERGED | integration agent | H01 | Rust crypto-core integrated after independent Codex review; ADR-0003 F-2/F-3 and H02 release gates remain |
| B02 | MERGED | backend agent | A02 | Health, redaction, and repeatable PostgreSQL migration tests pass; Compose uses host port 5434 |
| B03 | MERGED | integration agent | B01 | crypto-ffi native bindings, generated WASM package, and Worker adapter merged; native and WASM pass identical golden vectors, malformed-message and lifecycle tests pass; `crates/crypto-ffi` registered in root Cargo workspace. Known limitation: full browser password-unlock parity is gated on B04's OPAQUE transport and a persisted-wrapper ownership/serialization contract (see `packages/crypto-wasm/ARCHITECTURE-GAPS.md`) |
| B04 | MERGED | integration agent | B02,A02,H01,B03 | OPAQUE server binding, registration/login, session issue/rotation/logout, device register/list/revoke (with session-revocation cascade), and durable account-scoped rate limiting are merged. Independently re-verified 2026-09-04 against real WASM + PostgreSQL: `TEST_DATABASE_URL=… bun test apps/backend/test/auth` 14/14 green; boundary check green. **Human scope decision, 2026-09-04:** WebAuthn MFA and `/account/recovery-reset` are explicitly descoped from B04 and remain separate security-sensitive follow-up work. They require a new ADR/human approval because the OpenAPI WebAuthn surface is absent and G-10 has not chosen the recovery-code encoding or `recoveryProof` verification. |
| B05 | MERGED | integration agent | B02,B04,A02 | Mutations (`baseRevision` optimistic concurrency), `mutationId` idempotency (advisory-lock-guarded, replay returns the exact original response), monotonic per-vault change feed with opaque cursor, tombstone retention, and vault ownership (generic 404, no existence oracle) implemented in `apps/backend/src/sync/`. New migration `003_sync.sql` adds `vaults`, `vault_items.sequence`, and `mutation_outcomes`. Verified: `bun test apps/backend/` 31/31 against real PostgreSQL, `cargo test --workspace`, boundary check. Known limitation: no baseRevision-conflict shape exists for a nonexistent item (contract's `Conflict.current` is non-nullable) — treated as 400, not 409; no tombstone purge/expiry implemented (indefinite retention, flagged as a product follow-up). |
| C01 | MERGED | integration agent | B03,B05,C02 | Full web vault MVP: onboarding (OPAQUE registration + `create_account_setup` key hierarchy + mandatory recovery-kit reveal/confirm, ADR-0008 §2 provisional hex labeling), unlock/lock via the Worker (lock verifiably clears the key session, item cache, search index, clipboard timer), CRUD for login/note/TOTP items, in-memory search, CSPRNG password generator, RFC 6238 TOTP via `crypto.subtle` only (ADR-0008 §3), `IndexedDBLocalRepository`, import preview + encrypted-by-default export with a typed-confirmation + re-authentication gate for plaintext export, client-side conflict diff, and device management. `packages/crypto-wasm`/`packages/crypto-worker` gained two new additive exports/message types (`create_account_setup`, `unlock_item_session_with_recovery`) per ADR-0008 — B03/C02's existing exports untouched. Verified (re-run independently after merge, including a `bun install` to link two new dev-only deps): `bun run --filter @zkpm/web test` 48/48, `bun run --filter @zkpm/web build`, `bun run --filter crypto-wasm test` 9/9, `bun run --filter crypto-worker test` 9/9, boundary check, `cargo test --workspace`. New dev-only dependencies (not in the production bundle): `happy-dom`, `fake-indexeddb`. No production UI framework added (hand-rolled `h()`/`mount()` helper). Known limitations: MVP uses one vault-wide `ItemKey` rather than per-item key issuance (deliberate, stayed inside ADR-0008's grant — see `packages/crypto-wasm/src/lib.rs`'s `create_account_setup` doc comment); no UI affordance yet for a user to find their own client-generated `accountId` for a second-device sign-in; `packages/sdk`'s `ApiClient` has no way to reconfigure `fetchImpl` post-construction. |
| C02 | MERGED | integration agent | A02,B03,B05 | ADR-0007 added client-side OPAQUE exports to `packages/crypto-wasm` (mechanical, no new cryptography). `packages/sdk` (`ApiClient`, `AuthClient`, `SyncEngine`, `LocalRepository`), `packages/domain` (normalized item type, strict exact-origin matching per T06), and `packages/importers` (CSV, Bitwarden, 1Password-CSV, Enpass normalizers, adversarial-input-safe per T16) are implemented and tested, including a live e2e test against the real backend + real WASM. Verified: `bun run --filter crypto-wasm test` 7/7, `bun run --filter @zkpm/sdk test` 32/32 (DB-gated e2e included), `@zkpm/domain` 10/10, `@zkpm/importers` 14/14, boundary check, `cargo test --workspace`. No new external dependency. Known limitations: 1Password's 1PUX (zip) format not implemented (only classic CSV); `LocalRepository` ships only an in-memory reference implementation (a persistent backend is C01's job); no client methods for `/account/key-bundle`/`/account/delete` yet. |
| C04 | REVIEW | integration agent | B03,B04,C01,C02 | **Human security approval, 2026-09-05:** ADR-0011 is approved and supersedes ADR-0009/ADR-0010 as implementation authority. Approved G1/G2 foundation is integrated in merge commits `945f8a2`, `3aafa08`, `eeda49f`, `8f12d30`, `470a6c1`, plus C04 implementation merges. Delivered: canonical account-bundle validation and CAS routes, forward-only device enrollment/provenance migrations, SDK codec/client methods, web publication/account-ID view, and extension authority/CSP hardening. Verification: backend worker suite 76/76, contract/SDK/extension integration 91/91, extension/adapter suite 32/32, web suite 55/55, package boundary check, and web build pass. The initial online-only bundle replay residual remains human-accepted. Review remains open only for the mandated real Chrome/Firefox security E2E evidence before MERGED. |
| C03 | MERGED | integration agent | C01,C02 | **MVP scope decision, 2026-09-04:** merged as the browser-extension security baseline only. Chrome/Firefox manifests, minimum-permission review tests, background-only volatile session abstraction, popup/content boundary, and fail-closed exact-origin/form policy are implemented and verified by extension tests/build/boundary check. Full extension unlock, encrypted save/update, TOTP, store submission, and browser E2E move to C04; no unsafe web-origin bridge was introduced. |
| C05 | MERGED | integration agent | C01 | React + strict TypeScript migration is merged in `c04f347`: typed React onboarding, unlock, vault, import/export, device, and conflict views preserve C01's Worker/WASM and session boundaries. Independent verification on 2026-09-04: `bun run --filter @zkpm/web test` 52/52, build, lint/typecheck (no `apps/web` diagnostics), boundary, and security gates pass. |
| Q01 | MERGED | integration agent | B04,B05,C01,C02 | Release-gate harness in `75e00f5` independently verified on 2026-09-04: migrations; real-WASM/PostgreSQL two-device offline/reconnect/tombstone E2E; PostgreSQL 16 dump inspection; isolated backup/restore; `bun audit`; security, lint, and boundary gates all pass. Q01-001 (external audit), Q01-002, and Q01-003 remain documented and human-accepted residual risks for personal/internal deployment only; Q01-001 blocks public H02/R01. |
| H02 | BLOCKED | external auditor | Q01 | Public-release audit remains blocked; unaffected by the R01 scope decision below (`H02-AUDIT-READINESS-REVIEW.md`). |
| R01 | DEPLOYED (personal-deployment scope) | integration agent | H02 (public release only) | **Human scope decision, 2026-09-04 (release owner):** for personal/internal deployment only (per `H02-RESIDUAL-RISK-ACCEPTANCE.md`), R01 may proceed without waiting on H02, scoped to deploying the backend API + PostgreSQL to a single VPS the owner controls, with TLS and backup/restore in place. Full staged rollout (EU multi-stage, Terraform-managed cloud, public release checklist) remains `BLOCKED` on H02. Sub-tasks merged: R01-VPS-DEPLOY (`085d1b3`, codex), R01-VPS-BACKUP (`c21fec9`, claude); a follow-up commit `4f38e71` fixed bare-IP TLS (see below). **Live on 2026-09-05:** integrator deployed to the owner-provided VPS (`root@31.56.113.48`, no domain yet) — ufw (22/80/443 only), Docker, `docker-compose.prod.yml` stack (postgres+migrate+api+caddy) all healthy, `/health/ready` returns 200 over HTTPS from a real client (curl, no forced SNI) via `HTTP/2`, port 80 redirects to 443, ports 5432/3000 confirmed unreachable from outside. **Found and fixed live**: the original `{$DOMAIN} { tls internal }` Caddyfile only worked for tools that force SNI onto an IP literal (`openssl s_client -servername`) — real clients (curl, Python `ssl`, browsers) never send SNI for a literal IP per RFC 6066, so Caddy had no matching TLS policy and returned a bare `internal_error` alert; further, `tls internal` with a host-unrestricted site address never issues a leaf cert at all (no SNI to key off). Fixed by splitting `CADDY_SITE_ADDRESS` (`:443`, host-unrestricted) from `CADDY_TLS_DIRECTIVE`, now a statically generated self-signed cert file (`infra/generate-selfsigned-cert.sh`) loaded via Caddy's `tls <cert> <key>`, sidestepping SNI-keyed issuance entirely. Known limitation: browsers will show an untrusted-certificate warning until a real domain is pointed at the VPS and the deployment is switched to `CADDY_SITE_ADDRESS=<DOMAIN>`/automatic Let's Encrypt. OPAQUE_SERVER_SETUP was generated on-host and never left the VPS/left this session's terminal output. `pass-backup.timer` is installed and enabled on the VPS (daily 03:15 UTC + jitter); a manual run was verified end-to-end (`systemctl start pass-backup.service` produced `/var/backups/pass-vps/pass-20260904T210254Z.dump`, 16K). Still the release owner's job, not automated: copy that dump directory (and the `OPAQUE_SERVER_SETUP` value from `infra/.env.production`) off-host to durable storage — nothing currently ships backups off the VPS itself. **Web client deployed 2026-09-05** (`a3b8f34`): built C05's React web client locally (`bun run --filter @zkpm/web build`) and now serve it same-origin from the same Caddy site as the API — `apps/web/index.html` loads an optional `./config.js` (deploy-time only, not committed) that sets `window.ZKPM_API_BASE_URL=""` so the SDK's `ApiClient` issues same-origin relative requests; Caddy routes the API's own route prefixes (`/health*`, `/auth*`, `/vaults*`, `/devices*` — the complete set, cross-checked against `apps/backend/src/*/routes.mjs`) to `api:3000` and everything else via `file_server` from `apps/web/dist` (bind-mounted read-only; the app has no client-side router, so there's no path collision). Verified live: `/` returns the app shell, `/main.js`/`/config.js`/`/crypto_wasm_bg.wasm` serve with correct content-types (`application/wasm` for the WASM binary), `/health/ready` and `/auth/opaque/login` still reach the backend (401 for a bodyless login attempt, not 404). Known limitation: `apps/web/dist` is a local build artifact copied to the VPS by hand (`scp`), not rebuilt on the VPS itself and not part of any CI/release pipeline yet — redeploying a web-client change today means re-running the build locally and re-copying `dist/`. |
| D01 | BLOCKED | desktop agent | R01 (public release) | |
| D02 | BLOCKED | mobile agent | R01 (public release) | |
