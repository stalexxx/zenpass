# Live status

This file is maintained by the integration agent.

| ID | Status | Owner | Dependencies | Last result |
|---|---|---|---|---|
| A00 | MERGED | foundation agent | — | Scaffold builds; boundary check and Rust tests pass |
| A01 | MERGED | security agent | A00 | 20 threats mapped to mitigations and verification |
| A02 | MERGED | contracts agent | A00 | OpenAPI, schemas, fixtures, and sync state machine added |
| A03 | MERGED | coordinator | A00 | UX flows and accessibility states documented |
| A04 | REVIEW | integration agent | A01,A02 | A04 rework scope (G-01, G-02, G-04, G-09, G-11 — see `H01-CRYPTO-FIXTURES-VERIFIER-PLAN.md`) is complete: `packages/crypto-fixtures/` is a dependency-free Bun harness (`bun run --filter crypto-fixtures test:structure`/`test:unit` green; `test` correctly fails closed without an adapter); recovery placeholder resized to 48 B (G-09). **Remaining gaps are explicitly human H01 reviewer work, not A04 rework**: G-03 (wrap vectors — blocked, no authoritative external source exists, see `docs/security/G03-WRAP-VECTORS-BLOCKER.json`), G-05 (independent adapter run), G-06 (library selection), G-07 (Argon2id calibration evidence), G-10 (recovery-code display encoding). Ready for the H01 reviewer to re-examine and update ADR-0003/`H01-REVIEW-CHECKLIST.md`'s evidence map. |
| H01 | MERGED | integration agent | A04 | `stalexxx` approved with findings on 2026-09-03; ADR-0003 freezes v1 and defers F-2/F-3 to B01/A04/H02 |
| B01 | MERGED | integration agent | H01 | Rust crypto-core integrated after independent Codex review; ADR-0003 F-2/F-3 and H02 release gates remain |
| B02 | MERGED | backend agent | A02 | Health, redaction, and repeatable PostgreSQL migration tests pass; Compose uses host port 5434 |
| B03 | MERGED | integration agent | B01 | crypto-ffi native bindings, generated WASM package, and Worker adapter merged; native and WASM pass identical golden vectors, malformed-message and lifecycle tests pass; `crates/crypto-ffi` registered in root Cargo workspace. Known limitation: full browser password-unlock parity is gated on B04's OPAQUE transport and a persisted-wrapper ownership/serialization contract (see `packages/crypto-wasm/ARCHITECTURE-GAPS.md`) |
| B04 | IN_PROGRESS | integration agent | B02,A02,H01,B03 | ADR-0006 adds `packages/crypto-server` (OPAQUE server binding). OPAQUE register/login, session issue/rotate/logout, device register/list/revoke (cascades session revocation, ADR-0005 §2), and durable login rate-limiting (ADR-0005 §3) are implemented and pass a full HTTP-level round trip against real WASM + PostgreSQL (`bun test apps/backend/`, 21/21). **Not done, blocked pending human decisions (ADR-0006 Consequences):** WebAuthn MFA has no contract endpoints/schemas at all — needs a new ADR and human security approval before implementation; `/account/recovery-reset` needs the still-unsigned G-10 recovery-code-encoding decision (`docs/security/H01-RECOVERY-ENCODING-WORKSHEET.md`) before `recoveryProof` can be verified. `sessions.revokeAllSessionsForAccount` is ready for recovery-reset to call once that lands. B04 stays IN_PROGRESS, not MERGED, until MFA and recovery-reset are resolved or explicitly descoped by a human. |
| B05 | MERGED | integration agent | B02,B04,A02 | Mutations (`baseRevision` optimistic concurrency), `mutationId` idempotency (advisory-lock-guarded, replay returns the exact original response), monotonic per-vault change feed with opaque cursor, tombstone retention, and vault ownership (generic 404, no existence oracle) implemented in `apps/backend/src/sync/`. New migration `003_sync.sql` adds `vaults`, `vault_items.sequence`, and `mutation_outcomes`. Verified: `bun test apps/backend/` 31/31 against real PostgreSQL, `cargo test --workspace`, boundary check. Known limitation: no baseRevision-conflict shape exists for a nonexistent item (contract's `Conflict.current` is non-nullable) — treated as 400, not 409; no tombstone purge/expiry implemented (indefinite retention, flagged as a product follow-up). |
| C01 | IN_PROGRESS | web agent | B03,B05,C02 | ADR-0008 amends allowed paths to add `packages/crypto-wasm/`/`packages/crypto-worker/` (account-setup + recovery-kit WASM exports) and authorizes browser-native `crypto.subtle` TOTP; recovery-code display stays explicitly provisional pending G-10. Dispatched to a worktree agent 2026-09-04. |
| C02 | MERGED | integration agent | A02,B03,B05 | ADR-0007 added client-side OPAQUE exports to `packages/crypto-wasm` (mechanical, no new cryptography). `packages/sdk` (`ApiClient`, `AuthClient`, `SyncEngine`, `LocalRepository`), `packages/domain` (normalized item type, strict exact-origin matching per T06), and `packages/importers` (CSV, Bitwarden, 1Password-CSV, Enpass normalizers, adversarial-input-safe per T16) are implemented and tested, including a live e2e test against the real backend + real WASM. Verified: `bun run --filter crypto-wasm test` 7/7, `bun run --filter @zkpm/sdk test` 32/32 (DB-gated e2e included), `@zkpm/domain` 10/10, `@zkpm/importers` 14/14, boundary check, `cargo test --workspace`. No new external dependency. Known limitations: 1Password's 1PUX (zip) format not implemented (only classic CSV); `LocalRepository` ships only an in-memory reference implementation (a persistent backend is C01's job); no client methods for `/account/key-bundle`/`/account/delete` yet. |
| C03 | BLOCKED | extension agent | C01,C02 | |
| Q01 | BLOCKED | QA/security agent | C03 | |
| H02 | BLOCKED | external auditor | Q01 | |
| R01 | BLOCKED | release agent | H02 | |
| D01 | BLOCKED | desktop agent | R01 | |
| D02 | BLOCKED | mobile agent | R01 | |
