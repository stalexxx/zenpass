{
  "name": "@zkpm/backend",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun --check src/*.mjs",
    "dev": "bun --watch src/server.mjs",
    "start": "bun src/server.mjs",
    "migrate": "bun src/migrate.mjs",
    "test": "bun test",
    "lint": "bun --check src/*.mjs"
  },
  "dependencies": {
    "fastify": "^5.2.1",
    "pg": "^8.13.1"
  }
}
